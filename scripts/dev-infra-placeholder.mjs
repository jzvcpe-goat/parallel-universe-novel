#!/usr/bin/env node

console.log([
  'NarrativeOS infra placeholder:',
  '- Start PostgreSQL, Redis, and vector store before production-like runs.',
  '- Current first-round development can use existing local/mock storage.',
  '- Replace this placeholder with docker compose once infra files are finalized.',
].join('\n'))

